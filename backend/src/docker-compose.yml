version: '3.4'

services:
  # nichoshop.application:
  #   image: ${DOCKER_REGISTRY-}nichoshopapplication
  #   build:
  #     context: .
  #     dockerfile: NichoShop.Application/Dockerfile
  
  database:
    image: postgres

volumes:
  postgres_db: